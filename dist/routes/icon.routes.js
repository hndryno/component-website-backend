"use strict";
// import {Router, Request, Response, NextFunction} from 'express'
// import IconCreate from '../controllers/icon/create.controller';
// import multer from 'multer';
// import multerSettings from '../libs/multer';
// const upload = multer(multerSettings);
// const router: Router = Router()
// router.post('/', upload.single('icon'), async(req:Request, res: Response, next: NextFunction) => await new IconCreate().exec(req, res, next))
// export default router;
